#include<stdio.h>
int main()
{
    int w;
    prinf("enter the number from 1 to 10:");
    scanf("%d",&w);
    if(w%2==0)
    printf("giggle");
    else
    printf("not valid";)
    return 0;
}
